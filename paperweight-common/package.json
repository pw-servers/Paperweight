{
  "name": "paperweight-common",
  "version": "0.0.1",
  "main": "dist/common.js",
  "scripts": {
    "build": "rimraf dist/ && tsc",
    "prepublish": "npm run build",
    "test": "set DEBUG=pw:*&& jest"
  },
  "dependencies": {
  },
  "devDependencies": {
    "@types/jest": "^26.0.20",
    "jest": "^26.6.3",
    "ts-jest": "^26.5.0",
    "tslint": "^6.1.3",
    "typescript": "^4.1.3",
    "rimraf": "^3.0.2"
  }
}
